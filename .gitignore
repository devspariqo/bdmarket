node_modules/
.next/
out/
build/
dist/
.env
.env.local
.env*.local
# Local safety copies of the environment file
.env.backup
.env.local-backup
*.env.backup
*.log
npm-debug.log*
.DS_Store
Thumbs.db
prisma/dev.db
prisma/dev.db-journal
# Production SQLite database — never commit live data
prisma/prod.db
prisma/prod.db-journal
# Any other SQLite file (scratch databases, deploy probes, backups)
prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm

# Locally uploaded media (user content, not source)
public/uploads/

# Database dumps from scripts/db-transfer.js — contain customer PII
backup.json
*.db-backup.json
db-backup*.json

.vercel
*.tsbuildinfo
next-env.d.ts

# Deployment archives built for uploading to a host
bd-market-upload.zip
*-upload.zip
.upload-staging/

# AI assistant workspace notes — internal working notes, not project source.
# These contain the demo admin credentials and infrastructure detail, so they
# stay on the developer's machine and never reach a public repo.
.workbuddy-ai/
